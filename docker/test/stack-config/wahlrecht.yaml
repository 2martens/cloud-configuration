services:
  wahlrecht:
    image: 2martens/wahlrecht:0.3.8
    pull_policy: always
    networks:
      - internal_net
    deploy:
      replicas: 1
    environment:
      MONGODB_CONNECTION_STRING: <wahlrecht_mongodb_connection_string>

networks:
  internal_net:
    external: true
